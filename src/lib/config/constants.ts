export const API_BASE_URL =
  process.env.API_BASE_URL ?? 'http://localhost:3000';

/** Web app origin for upgrade / billing fallbacks opened in the browser. */
export const WEB_APP_URL =
  process.env.WEB_APP_URL ?? 'https://gump.app';
